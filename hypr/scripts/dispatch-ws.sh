#!/bin/bash
# ============================================================================
# 切换到指定工作区（dispatch 包装）
# 用法: dispatch-ws.sh <工作区号>   例: dispatch-ws.sh 3
# ============================================================================
hyprctl dispatch "hl.dsp.focus({workspace=\"$1\"})"
