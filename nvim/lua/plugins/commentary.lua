-- ============================================================================
-- 插件：vim-commentary（快速逐行注释）
-- 作用：一键注释 / 取消注释。核心用法：
--   * 普通模式 `gcc`          → 注释/取消注释当前行
--   * 普通模式 `gc` + 移动范围 → 如 `gc2j` 注释下面两行
--   * 可视模式选中多行后按 `gc` → 逐行注释/取消注释（本需求的核心用法）
-- 再次执行相同的注释操作会取消注释（toggle）。
-- ============================================================================
return {
  "tpope/vim-commentary",
  lazy = false,   -- 启动即加载，保证 gc/gcc 随时可用
}
