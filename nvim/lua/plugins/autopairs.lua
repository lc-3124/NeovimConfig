-- ============================================================================
-- 插件：nvim-autopairs（自动成对补全）
-- 作用：输入 ( [ { " ' 等左括号时，自动补出对应的右括号，并把光标放在中间
-- ============================================================================
return {
  "windwp/nvim-autopairs",
  event = "InsertEnter",          -- 进入插入模式时才加载（惰性加载，加快启动）
  config = true,                  -- 使用插件默认配置，无需自定义
}
