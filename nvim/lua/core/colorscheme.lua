-- ============================================================================
-- 主题模块（colorscheme）
-- 设置整体配色方案。实际主题插件在 lua/plugins/colorscheme.lua 中配置，
-- 这里只负责在启动早期应用主题。
-- ============================================================================

-- 应用 Tokyo Night 主题（默认风格见插件配置中的 style = "storm"）
vim.cmd([[colorscheme tokyonight]])
